.row
  - if !@products.empty?
    -@products.each do |product|
      .col-12.col-sm-4
        .card.my-3
          .card-body
            .row
              .col-8.col-sm-4
                = link_to product do
                  %h4=product.p_name
              .col-4.col-sm-8
                - if !product.myimages.first.nil?
                  = link_to product do
                    = image_tag product.myimages.first.img.url, class:"img-fluid"

  -else
    .col-md-8.offset-md-2
      .card.my-3
        .card-body
          %h1.text-center No existen coincidencias